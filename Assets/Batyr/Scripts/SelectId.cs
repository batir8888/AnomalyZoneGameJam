public enum SelectId // "первый стат" для селекции/комбинаций
{
    // Tier1: фиксированные 5 штук
    Red = 0, Blue = 1, Green = 2, Purple = 3, Gray = 4,
    // Tier>=2: будут использовать resultId из корзин (см. ArtifactDataFactory)
}